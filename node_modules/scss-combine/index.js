#! /usr/bin/env node
var combine = require('./combine');
var arguments = process.argv.slice(2);

if (arguments.length !== 1) {
  console.log('Usage: scss-combine <path>');
  process.exit(0);
}

var path = arguments[0];

console.log(combine(path));
